# Railway Deployment Configuration

# Server port (Railway passes PORT env variable)
server.port=${PORT:8080}

# MongoDB Atlas connection string (replace with your actual URI)
spring.data.mongodb.uri=${mongodb+srv://dobhalsukrit_db_user:RckBdgicguKNW2wR@ecommerce-cluster.ft1cflk.mongodb.net/?retryWrites=true&w=majority&appName=ecommerce-cluster}

# JWT secret key (must be kept secure)
jwt.secret=${JWT_SECRET}

# CORS allowed origins (replace with your GitHub pages URLs)
spring.web.cors.allowed-origins=${https://sukritdobhal.com/}

# Active profile
spring.profiles.active=${SPRING_PROFILES_ACTIVE:railway}

# Enable cache for templates and static resources
spring.thymeleaf.cache=true

# Optional: Disable extra logging for production
logging.level.org.springframework=INFO
logging.level.com.ecommerce=INFO
